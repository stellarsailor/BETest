const privateRoutes = {
  // 'POST /posts': 'TweetController.create',
  // 'PUT /posts/:id': 'TweetController.update',
  // 'DELETE /posts/:id': 'TweetController.deleteOne',
};

module.exports = privateRoutes;
